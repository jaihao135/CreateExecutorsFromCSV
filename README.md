# CreateExecutorsFromCSV
Batch-creates executors and timecode objects from a CSV list — a grandMA2 Lua plugin for automating repetitive per-item show setup (recital numbers, scenes, segments) instead of running a manual "create item" plugin one at a time.
